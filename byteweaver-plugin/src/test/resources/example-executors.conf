import java.util.concurrent.Executors;
import java.util.concurrent.ExecutorService;

class com.example.ExampleExecutors {
    void main(***) {
        static ExecutorService Executors.newCachedThreadPool() {
            replace ExecutorService com.example.ExampleExecutors.customCachedThreadPool();
        }
        static ExecutorService Executors.newFixedThreadPool(int) {
            replace ExecutorService com.example.ExampleExecutors.customFixedThreadPool(0);
        }
    }
}