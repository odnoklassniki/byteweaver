import java.lang.String;
import ru.ok.android.app.NotificationsLogger;
import android.app.NotificationManager;
import android.app.Notification;

class * {
    * *(***) {
        void NotificationManager.notify(int, Notification) {
            replace void NotificationsLogger.log(self, 0, 1);
        }
        void NotificationManager.notify(String, int, Notification) {
            replace void NotificationsLogger.log(self, 0, 1, 2);
        }
    }
}
